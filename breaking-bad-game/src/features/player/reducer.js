const initialsState = {
    position: [0, 0],
}

const playerReducer = (state=initialsState, action) => {

    switch(action.type){
        default:
            return state
    }

}

export default playerReducer